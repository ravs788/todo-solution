{
  "name": "tests-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright E2E tests for the todo application",
  "scripts": {
    "test": "npx playwright test --reporter=list,line,junit --output=./test-results",
    "test:headed": "npx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
